# Minimal requirements for Vercel deployment
# Using TensorFlow Lite instead of full TensorFlow to reduce size

# Core Dependencies (lightweight versions)
numpy>=1.21.0,<2.0.0
Pillow>=8.0.0,<11.0.0

# Web Framework
flask>=2.0.0,<4.0.0
werkzeug>=2.0.0,<4.0.0

# TensorFlow Lite (much smaller than full TensorFlow)
tflite-runtime>=2.14.0

# Note: For local development, use requirements.txt
# This file is optimized for Vercel's 250MB limit
