# Minimal requirements for Vercel deployment
# Using ONNX Runtime (lightweight ~50-80MB, supports Python 3.12)

numpy==1.26.4
Pillow==10.3.0
flask==3.0.3
werkzeug==3.0.3
onnxruntime==1.17.0
tf2onnx==1.16.1

# Note: onnxruntime is ~50-80MB vs tensorflow ~400MB
# This ensures we stay under Vercel's 250MB limit
# tf2onnx is needed to convert the model (can be in dev requirements later)
