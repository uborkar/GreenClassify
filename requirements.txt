# Minimal requirements for Vercel deployment
# Using TensorFlow 2.18.0 (supports Python 3.12)

numpy==1.26.4
Pillow==10.3.0
flask==3.0.3
werkzeug==3.0.3
tensorflow==2.18.0

# Note: tensorflow 2.18.0 is ~250MB but includes TFLite support
# We'll use the .tflite model file for inference (11 MB)
# Total should be just under 250MB limit
