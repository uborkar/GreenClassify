# Minimal requirements for Vercel deployment
# Using TensorFlow Lite with tensorflow-cpu (lighter than full tensorflow)

numpy==1.26.4
Pillow==10.3.0
flask==3.0.3
werkzeug==3.0.3
tensorflow-cpu==2.15.0

# Note: tensorflow-cpu is ~200MB vs full tensorflow ~400MB
# Still under Vercel's 250MB limit
# We'll use the .tflite model file for inference
