# Minimal requirements for Vercel deployment
# Using TensorFlow Lite Runtime ONLY (ultra-lightweight ~10MB)

numpy==1.26.4
Pillow==10.3.0
flask==3.0.3
werkzeug==3.0.3
tflite-runtime==2.14.0

# Note: tflite-runtime is ~10MB vs tensorflow ~400MB
# This ensures we stay under Vercel's 250MB limit
